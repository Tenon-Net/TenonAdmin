---
name: create-crud-backend
description: 为已有实体创建后端 CRUD 全套六件产出(Models/Interface/Service/ErrorCode/DI/Controller)+ 菜单种子取号规则。当用户要给实体加后端接口、写服务和控制器时使用。
---

读仓库根目录的 `skills/create-crud-backend.md` 并严格按它执行。它是单一真源,本文件只是入口包装。实体还没建先走 `skills/create-entity.md`。
