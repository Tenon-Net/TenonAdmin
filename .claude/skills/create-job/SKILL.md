---
name: create-job
description: 给自己的模块加定时任务——写 IAdminJob、TryAddEnumerable 注册一行、后台建任务或写种子;还有不用发版的 HTTP/SQL 任务与五个常见坑。当用户要加定时任务、周期任务、cron 调度、后台自动跑的活时使用。
---

读仓库根目录的 `skills/create-job.md` 并严格按它执行。它是单一真源,本文件只是入口包装。
