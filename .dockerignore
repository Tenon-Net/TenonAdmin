# 此列表首先是一项**安全**措施，其次才是关于镜像大小。
#
# backend/samples/MinimalHost/data/ 中存有真实的开发环境 admin.db 和 dev-jwt.key（仓库根的 data/ 同理）。
# 简单的 COPY . . 会**将签名密钥和开发数据库烘焙进镜像层**——一旦镜像被推送，
# 任何人都能提取它并伪造超级管理员令牌。因此排除 data/ 和 *.db* 不是可选的。

.git/
.github/
.vscode/
.idea/

# 构建产物
**/bin/
**/obj/
web/node_modules/
web/dist/
web/test-results/
web/playwright-report/
artifacts/

# 运行时数据（密钥/数据库/上传——绝不进入镜像）
**/data/
**/*.db
**/*.db-journal
**/*.db-*
**/wwwroot/upload/

# 本地开发配置（包含凭据）
**/appsettings.Development.json
.env.local

# 工具与 Agent 状态
.omc/
.omx/
.codegraph/
.claude/
.dev/

# 文档与脚本（镜像中不需要）。README.md 不排除：backend/Directory.Build.props 将其用作
# 包描述页——排除它会在构建中埋下"文件未找到"的地雷，省下的几 KB 不值得。
docs/
dev.bat
stop.bat
