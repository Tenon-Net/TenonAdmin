# Normalize line endings: always LF in the repo, auto-converted per platform on checkout
* text=auto eol=lf

# Explicitly mark binary/asset types to avoid accidental modification
*.png binary
*.jpg binary
*.ico binary
*.db binary
*.woff binary
*.woff2 binary
