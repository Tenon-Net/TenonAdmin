name: backend-release

# 在 **main** 上打 tag(如 v0.1.0)即打包核心 NuGet 包 + dotnet new 模板包并推 nuget.org(设计 §T10)。
# 开发在 dev,发版走 main:先把 dev 合进 main,再在 main 上打 tag。tag 打在别处会被下面的闸门拒绝。
# 版本从 tag 名推导(去掉前缀 v)。
#
# 发布凭证走 nuget.org 的 **Trusted Publishing(OIDC)**,不用长期 API Key:
#   - GitHub 用 id-token 换一个短期(约 15 分钟)的 nuget key,发完即失效 —— 无密钥可泄漏、无到期需轮换。
#   - 需在 nuget.org 建一条 Trusted Publishing 策略(Owner=Tenon-Net、Repo=TenonAdmin、
#     Workflow=backend-release.yml),并把下方 NuGet/login 的 `user` 换成拥有这些包的 nuget.org 账号名。
#
# 推包是**不可撤销**的(nuget.org 只能 unlist,不能删),所以 push 之前必须先过闸门:
# 构建 → 测试 → 模板冒烟(装消费者的第一条命令跑一遍)。三者任一红,就不发。
on:
  push:
    tags: ['v*']

jobs:
  # 闸门。没有它的时候,"测试是红的"和"模板 restore 不了"都能一路发到 nuget.org。
  verify:
    runs-on: ubuntu-latest
    steps:
      # fetch-depth: 0 —— 下一步要判断 tag 的 commit 在不在 main 上,浅克隆看不到历史。
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 发版分支是 main。tag 是分支无关的:在 dev(甚至任意分支)上打 v0.1.0 一样会触发这个工作流,
      # 于是没进 main、没被审的代码可以直接推到 nuget.org —— 而推包不可撤销。
      # 所以这里把"发版必须来自 main"从约定变成闸门。
      - name: Tag must be on main
        run: |
          if ! git merge-base --is-ancestor "$GITHUB_SHA" origin/main; then
            echo "::error::标签 $GITHUB_REF_NAME 指向的提交 $GITHUB_SHA 不在 main 上。先把 dev 合进 main,再在 main 上打 tag。"
            exit 1
          fi

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Build
        run: dotnet build backend/TenonAdmin.slnx -c Release

      # SQLite 腿即可:多库矩阵由 backend-ci 在合入前守;这里守的是"发出去的东西自己能不能过"。
      - name: Test
        run: dotnet test backend/TenonAdmin.slnx -c Release --no-build

      # 消费者的第一条命令:dotnet new tenon-app → restore → build。
      # 它按 tag 版本打包并让模板引用同一版本,所以"模板引用了一个 tag 没发布的版本"这类错在这里当场暴露。
      - name: Template smoke (dotnet new tenon-app must restore + build)
        shell: pwsh
        run: templates/smoke-test.ps1 -Version ($env:GITHUB_REF_NAME -replace '^v', '')

  pack-push:
    needs: verify
    runs-on: ubuntu-latest
    # id-token: write —— Trusted Publishing 用它签发 OIDC 令牌换取短期 nuget key。缺了这行 NuGet/login 拿不到令牌。
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Resolve version from tag
        id: ver
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Pack
        run: dotnet pack backend/TenonAdmin.slnx -c Release -o artifacts -p:Version=${{ steps.ver.outputs.version }}

      # 模板包在 backend/ 之外(不继承 net10/中央版本管理),需单独 pack;落同一 artifacts 目录随下方一并推。
      # 打包时会把这个版本号盖进模板的 TenonPkgVersion 默认值(见 TenonAdmin.Templates.csproj 的 StampTemplateVersion)。
      - name: Pack templates
        run: dotnet pack templates/TenonAdmin.Templates.csproj -c Release -o artifacts -p:Version=${{ steps.ver.outputs.version }}

      # 用 GitHub 的 OIDC 令牌换一个短期 nuget key。`user` 必须是 nuget.org 上建了 Trusted Publishing 策略、
      # 且拥有(或被授权 push)这些包的账号名。换来的 key 落在 NUGET_API_KEY 环境变量里,仅本次运行有效。
      - name: NuGet login (Trusted Publishing / OIDC)
        uses: NuGet/login@v1
        id: login
        with:
          user: hu531035580

      # .snupkg 符号包与同名 .nupkg 一起自动推送,无需单列 glob。
      - name: Push to nuget.org
        run: dotnet nuget push "artifacts/*.nupkg" -k "${{ steps.login.outputs.NUGET_API_KEY }}" -s https://api.nuget.org/v3/index.json --skip-duplicate
