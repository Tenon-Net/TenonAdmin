## .NET
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user
.vs/
artifacts/
*.nupkg
*.snupkg

## App runtime data
data/
*.db
*.db-journal
# 文件日志(TenonAdmin:Logging:File,默认关);无前导斜杠 → 匹配任意宿主下的 logs/
logs/
# 带中间斜杠的模式会被锚定到仓库根;用 **/ 前缀才能匹配任意宿主(如 samples/*/wwwroot)下的上传目录
**/wwwroot/upload/

## Backend 本地开发配置(含种子凭据,不入库;用 .example 模板 + dotnet user-secrets)
backend/**/appsettings.Development.json

## Frontend (web/)
node_modules/
dist/
.env.local
# Playwright 产物:web/.gitignore 已忽略 web/ 下的同名目录,但从仓库根跑会漏到这里
test-results/
playwright-report/

## Tooling / agent state (本地工具产物,不入库;无前导斜杠 → 匹配任意层级,含 backend/.omc)
.omc/
.omx/
.codegraph/
# 本地 Claude Code 钩子脚本:只有被同样不入库的 .claude/settings.json 引用才生效,单独提交无意义。
# 若哪天要让全体协作者都吃到某条钩子(如"版本号只准写进 Directory.Packages.props"),
# 就得 hooks/ + settings.json 成套提交,届时再解掉这行。
.claude/hooks/
.claude/launch.json
.claude/settings.local.json

## Backend 临时验证脚本
backend/scratchpad/

## dev scripts (mac/linux) runtime
.dev/

## 同步工具冲突副本 / 传输临时文件(Syncthing 等生成,本地垃圾文件)
*.sync-conflict-*
~syncthing~*
