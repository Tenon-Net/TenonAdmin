# CONTEXT.md — 领域术语表

> 本文件是术语表,只回答「这个词在本仓精确指什么」,不含实现细节(实现去各模块台账)。按 `docs/agents/domain.md` 的懒创建约定,首批词条来自定时任务模块(2026-07-26,`docs/scheduling-ledger.md`)。

## 定时任务(Scheduling)

| 术语 | 定义 |
|---|---|
| 任务(Job) | `sys_job` 一行:触发配置 + 载荷 + 失败策略 + 运行状态的完整声明。一个任务恰有一份触发配置。 |
| 触发配置(Trigger) | 任务行上的调度规则(Cron / 固定间隔 / 一次性 + 生效窗口)。合并进任务行,不是独立实体——本仓不说「触发器」。 |
| 载荷 / 处理器(Handler) | 任务被触发时真正执行的东西:编译类(`IAdminJob` 实现)、HTTP 请求、SQL 语句三类。 |
| 属性包(PropsJson) | 任务行上的字符串字典 JSON,处理器参数的唯一入口(HTTP 的 url/headers、SQL 的语句、编译类的自定义参数)。 |
| 执行记录(JobLog) | `sys_job_log` 一行 = 一次执行尝试;`EndTime` 为空即「运行中」。 |
| 一次触发(FireInstance) | 一个到期时刻被领取后的完整执行过程(含全部重试),同一 `FireInstanceId` 聚合多行执行记录。 |
| 领取(Claim) | 触发前对任务行 `NextRunTime` 的原子 CAS 更新;防双发的**唯一**正确性来源(租约只管效率)。 |
| 主节点(Leader) | 持有 `sys_job_lock` 租约的节点,唯一扫表发起调度者;其余节点为备(standby)。 |
| 心跳(Heartbeat) | 每节点每 10s 一次:upsert 自己的 `sys_job_node` 行;主节点续租,备节点尝试夺租。 |
| 租约(Lease) | `sys_job_lock` 行上的 `LeaseUntil`;过期未续即可被备节点夺取(默认 30s)。 |
| 错过 / 补偿(Misfire) | 到期时刻迟到超过阈值(默认 60s,重启/切主/卡顿所致)。Skip = 不补跑只推进(默认);FireOnceNow = 立即补跑一次,错过再多也只补一次。 |
| 崩溃(Panic) | 任务连续失败达阈值后的停摆态:不再调度、已发过告警,等人工恢复。 |
| 串行跳过(SerialSkip) | 默认并发模式:上次触发未结束则本次跳过并记 Skipped 记录;另一模式为并行(Parallel),无排队。 |
| 执行一次(Run-now) | 手动触发:在收到请求的副本本机执行,不经选主、不做领取、不动 `NextRunTime`。 |

## 通用(既有约定,收录防歧义)

| 术语 | 定义 |
|---|---|
| 内核 | `backend/src/TenonAdmin.{Core,SqlSugar,Services,AspNetCore}` 四包 + 元包 `TenonAdmin`。 |
| 卫星包 | 可选独立 NuGet 包(`TenonAdmin.Excel`、`TenonAdmin.Caching.Redis`、`TenonAdmin.Auth.*`),装了才有。 |
| 消费者 | 安装这些包搭自己后台的开发者;其程序集经 `options.ApplicationAssemblies` 挂入。 |
| 六件套 | `ReplaceabilityTests` 锁死的可替换性契约:内置服务 TryAdd 注册、前置注册即胜出、方法 `virtual` 可继承覆写。 |

## 等保应用安全能力

| 术语 | 定义 |
|---|---|
| 定级对象 | 实际承载业务、数据和用户的已部署信息系统。等保级别由其受侵害客体和影响程度决定;TenonAdmin 内核本身不是定级对象。 |
| 等保应用安全能力底座 | TenonAdmin 内核提供的身份鉴别、访问控制、安全审计、会话安全、数据权限等应用层控制集合。它可支撑消费者建设等保系统,但不等同于某个部署实例已通过等保测评。 |
| 内核符合性 | 对内核是否原生提供某项应用安全控制、默认是否启用、能否仅通过配置或扩展点达到要求的判断。不涵盖网络、主机、机房、数据库运维、制度与测评证据。 |
| 开箱满足 | 消费者不改内核源码且不额外开发安全组件,采用安全默认配置即可获得该控制。生产密钥、域名、日志目的地等环境值仍需部署时提供。 |
| 配置后满足 | 内核已有完整控制,消费者只需通过 Options、配置中心或宿主接线启用并设置参数,无需修改内核源码。 |
| 扩展后满足 | 内核已有稳定接口和接线位置,但消费者必须提供外部实现或基础设施,例如真实短信通道、集中日志平台或对象存储。只存在扩展点不算内核已经实现该控制。 |
| Level3 安全档 | 生产环境可显式启用的 `TenonAdmin:Security:Profile=Level3`。内核在启动时验证三级应用安全基线的强制项;配置不合格即拒绝启动。Development 不自动启用,保留低摩擦开发体验。 |
| 强制 MFA 账号 | Level3 安全档下必须在密码之外完成第二因子验证的账号集合:超级管理员始终属于该集合;拥有高敏感操作权限的账号自动属于该集合;管理员还可对任意用户显式要求 MFA。第二因子未启用、未绑定或不可用时,登录必须拒绝,不得降级直通。 |
| 用户 MFA 强制标记 | 用户记录上的显式安全策略。启用后,无论该用户是否为管理员或持有高敏感权限,登录都必须完成 TOTP。它只能增加 MFA 要求,不能覆盖 Level3 的自动强制规则。 |
| 高敏感权限集合 | 用于自动判定强制 MFA 的权限码集合。内核维护不可移除的默认下限;消费者可在受保护页面动态追加或移除自定义项,但不得降低内核默认项。变更写入操作审计,并在下一次登录时生效。 |
| Level3 TOTP 初始化授权 | 仅在首次启用 Level3 后，用于首个超级管理员绑定 TOTP 的一次性、短时授权。部署者通过环境变量或密钥管理注入;绑定成功后立即失效。其他管理员的绑定或重置必须由已完成 TOTP 的管理员在受审计的后台流程中发起。 |
| Level3 审计留存 | Level3 下操作、登录和异常审计日志的最短留存期为 180 天。留存配置只能调长,不能调短;不提供人工一键清空,仅按策略分批清理到期数据。导出、敏感日志查询及留存策略变更本身必须留审计。 |
| 审计外送 | 内核提供的可替换审计事件输出接口,用于由消费者接入 SIEM、日志平台或 WORM 存储。外部集中与防篡改存储由部署方负责,但内核不得阻断或遗漏该集成路径。 |
| 安全 Cookie 会话模式 | 刷新令牌仅保存在 `HttpOnly`、`Secure`、`SameSite` 受控的 Cookie 中,JavaScript 不可读取;访问令牌仅在浏览器内存短暂保存。页面重载时由 Cookie 静默换取新的访问令牌。Level3 强制此模式,并要求 CSRF 防护与显式可信来源配置。 |
| Level3 浏览器安全响应头 | Level3 生产环境强制 HTTPS,并统一输出 HSTS、CSP、防嵌入、禁止 MIME 嗅探、Referrer Policy 和 Permissions Policy。CSP 默认仅允许同源资源;消费者只能显式追加必要的可信来源。 |
| 文件安全扫描器 | 内核可替换的 `IFileSecurityScanner`，在文件入库和对外访问前判定是否安全。Level3 下必须使用真实扫描器;扫描失败、超时或未配置均拒绝，开发用空实现不能通过 Level3 启动校验。扫描结果、引擎版本和拒绝原因进入审计日志。 |
| Level3 密码与登录防护基线 | 最小 12 位，四类字符中至少三类，最近 5 次密码不可复用；初始和重置密码均强制首次修改，最长有效期 90 天；连续失败 5 次至少锁定 15 分钟。图形验证码在连续失败后成为强制门槛，不能被永久关闭。 |
| 数据保护密钥提供方 | 内核的 `IDataProtectionKeyProvider` 契约，用于从 KMS、密钥管理服务或受控密钥文件取得带版本号的加密密钥。主密钥不得硬编码在代码或存入业务数据库。 |
| 字段保护器 | 内核的 `IFieldProtector` 契约，对被标记的敏感字段实施带版本号的认证加密，并支持密钥轮换后的渐进重加密。需要精确查询时另存不可逆的规范化哈希索引。 |
| 安全事件接收端 | 内核的 `ISecurityEventSink` 契约，接收结构化、可关联且已脱敏的安全事件，用于由消费者接入 SIEM、Webhook、邮件或告警平台。Level3 必须配置实际接收端，不能仅写本地日志。 |
| 高危操作再次认证 | 对 MFA 重置、管理员与权限变更、安全策略、审计策略、密钥配置、敏感审计导出和安全集成变更等极高风险操作，已登录用户须再次完成 TOTP，必要时再输入当前密码。通过仅对同类操作短时有效，默认约 5 分钟。 |
| 国密密码档 | `CryptoProfile=GM` 的可选密码策略档，用于需要商用密码应用合规的消费者。Level3 不自动等同于国密；启用该档时，内核必须校验相应的算法实现、TLS/证书路径和密钥来源均满足国密要求。 |
| 发布供应链基线 | 内核正式发布时生成 SBOM，并扫描 NuGet、npm 与容器基础镜像依赖。PR 以告警反馈；正式发布对未获批准的 Critical/High 漏洞阻断。例外必须记录理由、责任人和到期日，过期后重新阻断。发布产物与 SBOM 提供可验证的签名或构建证明。 |
| 安全事件 Outbox | 安全动作与其结构化事件在同一数据库事务中持久化，后台任务异步投递给 `ISecurityEventSink`。投递失败指数退避重试；持续失败产生本地高优先级事件并使 `/health/ready` 不就绪，但不因外部平台短暂故障阻断普通用户请求。 |
| Level3 预检与证据报告 | 对 Level3 强制项执行启动期校验，并提供给高敏感管理员、导出报告和 CI/部署流水线的脱敏结果。报告仅含通过/失败项、配置来源和修复建议，不输出密钥、令牌、连接串或 TOTP 秘钥。 |
| Level3 显式启用 | `TenonAdmin:Security:Profile=Level3` 必须由消费者显式设置，默认不启用，避免既有项目仅升级内核即被安全档的启动校验阻断。Production 未启用时内核明确告警，预检报告标记为未满足三级应用安全基线。 |
| Level3 高危定时任务 | Level3 默认只允许受控的编译型任务;SQL 任务和外部 HTTP 任务须由部署配置分别显式启用。创建、修改和执行这些任务仅限超级管理员，须短时再次认证，并产生完整审计与安全事件。HTTP 任务强制域名白名单和内网地址阻断；SQL 任务仅能使用指定数据库连接，不接受任意连接串或凭据。 |
| Level3 MFA 恢复 | 用户可使用一次性恢复码恢复 TOTP。管理员重置 MFA 只生成短时、一次性的重新绑定邀请，不能直接解除 MFA。超级管理员 MFA 重置须由另一名已完成 TOTP 的超级管理员批准；只有一个超级管理员时，只能使用部署期紧急恢复授权，并生成最高级安全事件。全程需要再次认证、审计与安全事件。 |
| Level3 三期交付 | 第一期交付认证、会话和预检闭环；第二期交付审计与应用防护；第三期交付字段保护、国密档和发布供应链。每期均保留可验证的配置、测试和迁移路径，消费者可逐期接入。 |
| Level3 MFA 迁移 | 启用 Level3 后，尚未绑定 TOTP 的强制 MFA 用户不得正常登录，只能处于等待管理员发放绑定邀请的受限状态。首个超级管理员用部署期初始化授权绑定；其余用户由已完成 TOTP 的管理员发放短时、一次性邀请，避免持有旧密码者抢先绑定认证器。 |
| TOTP 绑定邀请 | 内核生成短时、一次性的绑定链接或二维码，管理员可经既有安全渠道交付给用户；消费者已有邮件或短信实现时可选择自动发送。内核不依赖具体通知厂商，邀请使用后立即失效。 |
| TOTP 绑定邀请生命周期 | 绑定邀请默认有效 15 分钟，管理员可撤销；同一用户重新发放会自动使旧邀请失效。邀请只允许完成 TOTP 绑定，不授予普通系统访问权限。使用、过期、撤销和重复发放均写入审计和安全事件。 |
| TOTP 恢复码 | 每次绑定或重新绑定 TOTP 时生成 10 个一次性恢复码，仅展示一次，服务端只存哈希。码使用后失效，强制重新绑定 TOTP 并吊销该用户全部会话；生成新码即废弃旧码。下载/查看、使用与重新生成均写审计，Level3 下重新生成还须再次认证。 |
| 双提交 CSRF | Cookie 会话模式下的 CSRF 防护：刷新令牌保持 `HttpOnly`；后端下发可读的随机 `csrf` Cookie；前端在所有写请求附带 `X-Tenon-CSRF` 请求头；后端对二者做常量时间比对。CSRF 值在登录、刷新、提权认证和登出时轮换。 |
| Level3 Redis 安全状态 | Level3 无论单实例或多副本均使用启用认证和 TLS 的 Redis 保存短时挑战、登录锁定、限流与可失效会话/权限缓存。Redis 不保存明文密码、TOTP 秘钥或刷新令牌；不可用时 `/health/ready` 失败，且不得降级到内存缓存。 |
| 第三方接入应用 | 供程序、定时集成和外部系统调用 API 的独立机器身份。管理员为应用绑定最小权限、数据范围、有效期和可选 IP 白名单；可禁用、轮换密钥、限流和审计。浏览器用户不使用它，高敏感管理接口默认不授予它。未配置 IP 白名单不拒绝调用，但预检报告明确提示该加固项缺失。 |
| 第三方 HMAC 请求签名 | 第三方接入应用使用 `ClientId + ClientSecret` 对时间戳、随机数、方法、路径和请求体摘要进行 HMAC 签名。服务端验证签名、有效时间与随机数唯一性以防篡改和重放；`ClientSecret` 仅在创建或轮换时展示一次，服务端经 `ISecretProtector` 以信封加密保存，不能错误地只存哈希。 |
| Level3 安全档治理 | `TenonAdmin:Security:Profile=Level3` 仅从部署配置或密钥管理读取，不能在配置中心或页面运行时降级。进入或退出 Level3 都需要部署变更和重启；降级产生最高级安全事件、完整审计并在预检报告持续标记。 |
| 审计完整性链 | 操作、登录、异常和高危安全事件以追加式前序哈希形成可验证链，每日生成签名摘要并通过 Outbox 外送。预检或安全页面可检测链断裂及其时间段。该能力不阻止数据库最高权限者篡改，但使篡改可被发现。 |
| Level3 会话时限 | 普通用户空闲 30 分钟、强制 MFA 用户空闲 15 分钟后会话失效，绝对会话最长 8 小时，访问令牌最长 15 分钟。消费者只能配置得更短，不能放宽；前端在超时前提示续期。高危操作再次认证仍为独立的约 5 分钟窗口。 |
| Level3 并发会话 | 强制 MFA 账号默认只允许 1 个活跃会话，新登录吊销旧会话并产生安全事件；确有多终端需求时最多放宽到 2 个，预检报告提示风险。普通用户默认最多 3 个活跃会话。消费者可配置得更低。 |
| Level3 闲置账号 | 普通账号连续 90 天未登录自动停用；强制 MFA 账号 60 天告警、90 天自动停用。超级管理员不自动停用以避免锁死，但 60/90 天分别产生告警和高优先级安全事件。重新启用需要再次认证、审计，并吊销旧会话和绑定邀请。 |
| 敏感数据展示与导出策略 | 内核按字段敏感级别实施默认掩码、明文查看权限与导出控制。查看明文、批量导出及导出敏感字段均写审计;Level3 下敏感导出还须短时再次认证。消费者可通过属性或配置标记业务字段并选择掩码规则;导出可带操作者、时间和追踪 ID 水印。 |
| TOTP | 基于时间的一次性动态口令。用户用认证器应用扫描内核提供的绑定二维码,登录时在密码之后输入当前口令。Level3 的默认 MFA 合规路径;不依赖短信厂商。 |
