# 国际化

这页讲前端这套 vue-i18n 怎么接起来、日常怎么加一个翻译键、语言怎么在运行时切换。

## 怎么接起来的

`src/locales/index.ts` 把 vue-i18n 接起来：

```ts
export const i18n = createI18n({
  legacy: false,
  locale: 'zh-CN',
  fallbackLocale: 'en-US',
  messages: { 'zh-CN': zhCN, 'en-US': enUS },
})

/** 供非 setup 上下文(工具函数)使用的翻译器。 */
export const t = i18n.global.t
```

Composition API 模式（`legacy: false`），默认语言 `zh-CN`，当前语言缺键时回退 `en-US`。内置文案就两个文件——`locales/zh-CN.ts` 与 `locales/en-US.ts`，没有按组件拆分，也没有按需加载。

这两个是上游自留地。如果你是在 fork 上做业务，自己的文案请放 `locales/ext/<locale>/<模块>.ts`:`locales/index.ts` 会 glob 进来并深合并，文件名即顶层命名空间，你什么都不用注册。这样你的文案就不落在 `web/src` 里 churn 最高的文件上——见[同步上游](/zh/guide/sync-fork)和该目录下的 [README](https://github.com/Tenon-Net/TenonAdmin/blob/main/web/src/locales/ext/README.md)。

`App.vue` 把 Naive UI 自己的 locale（驱动日期选择器、分页等内置文案）与 app 的语言保持同步：

```ts
const naiveLocale = computed(() => (app.locale === 'en-US' ? enUS : zhCN))
const naiveDateLocale = computed(() => (app.locale === 'en-US' ? dateEnUS : dateZhCN))

watch(
  () => app.locale,
  (l) => {
    i18n.global.locale.value = l
  },
  { immediate: true },
)
```

所以实际上有两套语言状态从同一个源（`app.locale`）联动：vue-i18n 的 `i18n.global.locale`（驱动 `t()`）和 Naive UI 的 `n-config-provider :locale/:date-locale`（驱动它内部组件的文案）。

## 加一个翻译键

两个文案文件按功能命名空间分组——`common`、`app`、`settings`、`login`、`module`、`menu`、`user`、`config`、`dict`、`org`、`file`、`notice`、`role`、`log`、`profile`、`error`、`recycle` 等等。两个真实例子：

```ts
// zh-CN.ts
user: {
  title: '用户管理',
  account: '账号',
  deleteConfirm: '确定删除用户「{name}」?',
}

// en-US.ts
user: {
  title: 'Users',
  account: 'Account',
  deleteConfirm: 'Delete user "{name}"?',
}
```

加键步骤：

1. 找准命名空间（确实是新功能才新开顶级命名空间）。
2. 在 `zh-CN.ts` **和** `en-US.ts` 里同时加，同一个点分路径，插值占位符（`{name}`、`{count}` …）也要对上。
3. 组件内用 `t('命名空间.键')`;非 setup 上下文（如 `utils/` 里的函数）用导出的 `t`。

[前端规范](/zh/standard/frontend) 把这条定成硬规矩：视图内所有可见文本走 `t('...')`，禁止硬编码中文/英文字面量。

## 运行时切换语言

`stores/app.ts` 把语言当作持久化的 UI 偏好来管：

```ts
export type Locale = 'zh-CN' | 'en-US'

state: () => ({ ...DEFAULTS, collapsed: false, locale: 'zh-CN' as Locale }),

actions: {
  setLocale(l: Locale) {
    this.locale = l
  },
}
```

调用 `useAppStore().setLocale('en-US')` 更新 `app.locale`;整个 store 经 Pinia 持久化插件存进 `localStorage`（键名 `app`），刷新后语言选择还在。`App.vue` 里那个 `watch(() => app.locale, ...)`（见上文）随后把这个值推进 `i18n.global.locale.value`，而 `naiveLocale`/`naiveDateLocale` 这两个计算属性也从同一个 `app.locale` 派生——所以 vue-i18n 的文案和 Naive UI 内置文案在同一次渲染里一起切换。

后端错误码怎么落成 `error.*` 里的文案（数字码 → `msgKey` → 翻译键这套映射），不在这页，它和响应契约绑在一起，见 [对接后端](/zh/frontend/api-contract)。
