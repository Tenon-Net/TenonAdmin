# 第三方登录品牌图标（请自行放入）

与 `web/src/assets/oauth/README.md` **同样规则**（React 模板独立一份）。

## 文件名

`wechat` / `wecom` / `dingtalk` / `github` / `gitee` / `qq` 的 `.svg` 或 `.png`。

## 规格

- 首选 **SVG**，否则 **PNG ≥ 128×128**（推荐 256×256）  
- **彩色圆形完整标**（圆底画在图里，圆外透明）  
- 对齐 Gitee 登录页那排样式  

放好后重启/刷新 dev 即可。
